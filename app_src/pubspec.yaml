name: lumi
description: Lumi AI chat app (Flutter WebView shell).
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  webview_flutter: ^4.7.0
  webview_flutter_android: '>=3.15.0 <4.0.0'

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/index.html
